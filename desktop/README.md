# KanbanQube Desktop

Desktop wrapper for KanbanQube. It starts the KanbanQube server locally and opens it in an Electron window.

```sh
npx kanbanqube-desktop
```

Custom vault:

```sh
npx kanbanqube-desktop /path/to/your/vault
```
